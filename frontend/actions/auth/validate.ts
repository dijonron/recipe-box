// import { authClient } from "@/lib/grpc/authClient";

// export async function validate(token: string): Promise<any> {
//   return new Promise((resolve, reject) => {
//     authClient.Validate({ token }, (err: any, response: any) => {
//       if (err) reject(err);
//       else resolve(response);
//     });
//   });
// }
